# Language-neutral contracts

This directory will hold cross-language contract artifacts (Worker job/result, authorization decision, SecretReference, opaque artifact locator, correlation ids).

Serialization and validation strategy are not chosen yet. These are not Python modules and not vendor SDKs.
